{
  "name": "p-js-error",
  "version": "0.0.4",
  "scripts": {
    "build": "rm -rf lib && babel src -d lib"
  },
  "main": "lib/error.js",
  "dependencies": {},
  "babel": {
    "presets": [
      "env"
    ],
    "ignore": [
      "node_modules"
    ],
    "env": {
      "build": {
        "optional": [
          "optimisation",
          "minification"
        ]
      }
    }
  },
  "devDependencies": {
    "babel-preset-env": "^1.7.0"
  }
}
