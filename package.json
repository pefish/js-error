{
  "name": "@pefish/js-error",
  "version": "0.0.5",
  "scripts": {
    "build": "rm -rf lib && babel src -d lib",
    "prepublish": "yarn build"
  },
  "main": "lib/error.js",
  "dependencies": {},
  "babel": {
    "presets": [
      "env"
    ],
    "ignore": [
      "node_modules"
    ],
    "env": {
      "build": {
        "optional": [
          "optimisation",
          "minification"
        ]
      }
    }
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
